import { StyleSheet } from "react-native";

const stylesSituacao = StyleSheet.create({
    tdSituacao: {
        fontSize: 17,
        color: "#565656",
        borderBottomWidth: 2,
        alignSelf: 'flex-start',
        width: "auto",       
    },
})

export default stylesSituacao;